<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ประกาศมหาวิทยาลัย</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/css/style_announcement.css">
    <link rel="stylesheet" href="/css/user_btn.css">
    <link rel="stylesheet" href="/css/nav.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/card.css">
</head>
<body>
            <%- include('partials/nav') %>

            <section class="page-header">
                <div class="container">
                    <h1>ประกาศมหาวิทยาลัย</h1>
                    <p>ติดตามข่าวสารการรับสมัคร ทุนการศึกษา และโปรแกรมจากมหาวิทยาลัยชั้นนำ</p>
                </div>
            </section>

            <section class="search-filter-section">
                <div class="container">
                    <% const anns = (typeof announcements !== 'undefined' && announcements) ? announcements : []; %>
                    <div class="search-filter-wrapper">
                        <div class="search-bar">
                            <i class="fas fa-search" aria-hidden="true"></i>
                            <input type="text" id="search" aria-label="ค้นหาประกาศ" placeholder="ค้นหาประกาศ มหาวิทยาลัย หรือโปรแกรม..">
                        </div>

                        <div class="filters">
                            <div class="dropdown">
                                <select id="university-filter" aria-label="กรองตามมหาวิทยาลัย">
                                    <option value="">ทุกมหาวิทยาลัย</option>
                                    <% Array.from(new Set(anns.map(a=>a.university).filter(Boolean))).forEach(function(u){ %>
                                        <option value="<%= u %>"><%= u %></option>
                                    <% }) %>
                                </select>
                                <i class="fas fa-chevron-down"></i>
                            </div>

                            <!-- type filter removed because announcements do not include tag field -->
                        </div>
                    </div>

                    <div class="results-info">
                        <p><span id="count"><%= anns.length %></span> ประกาศทั้งหมด</p>
                        <div class="sort-by">เรียงตามวันปิดรับสมัคร <i class="fas fa-sort"></i></div>
                    </div>
                </div>
            </section>

            <section class="events-grid-section">
                <div class="container">
                    <div class="card-container" id="cards">
                        <% if(!anns.length){ %>
                            <p class="muted">ยังไม่มีประกาศในขณะนี้</p>
                        <% } else { %>
                            <% anns.forEach(function(a){ %>
                            <article class="card announcement-card" data-id="<%= a.id %>" data-university="<%= a.university %>" data-faculty="<%= a.faculty %>" data-department="<%= a.department %>" data-description="<%= (a.description||'').replace(/\"/g,'&quot;') %>">
                                <header class="card-header centered-header">
                                    <div class="tags">
                                        <span class="tag university"><%= a.university %></span>
                                    </div>
                                </header>
                                <main class="card-body card-body-no-desc">
                                    <h3 class="ann-title centered-title"><%= a.title %></h3>
                                </main>
                                <footer class="card-footer">
                                    <% if(a.id){ %>
                                        <a class="btn view-ann-btn" href="/announcement/<%= a.id %>" aria-label="ดูรายละเอียดประกาศ <%= a.title %>">ดูเพิ่มเติม</a>
                                    <% } else { %>
                                        <button class="btn view-ann-btn" aria-label="ดูเพิ่มเติม">ดูเพิ่มเติม</button>
                                    <% } %>
                                    <div class="heart-icon" role="img" aria-label="ชอบ">❤</div>
                                </footer>
                            </article>
                            <% }) %>
                        <% } %>
                    </div>
                </div>
            </section>

            <!-- Modal for announcement details -->
            <div id="annDetailModal" class="modal" style="display:none">
                <div class="modal-overlay"></div>
                <div class="modal-dialog">
                    <button id="annClose" class="modal-close" aria-label="ปิด">×</button>
                    <h2 id="annTitle">Title</h2>
                    <div class="ann-tags">
                        <span id="annUniversity" class="pill">University</span>
                        <span id="annFaculty" class="pill">Faculty</span>
                        <span id="annDepartment" class="pill">Department</span>
                    </div>
                    <hr />
                    <div class="ann-description">
                        <h3>Description</h3>
                        <p id="annDescription">...</p>
                    </div>
                </div>
            </div>

            <!-- <%- include('partials/footer') %> -->

            <script src="/js/announcement.js"></script>
        </body>
    </html>
