<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เข้าสู่ระบบ</title>
    <link rel="stylesheet" href="/css/auth_card.css">
</head>
    <body>
        <main class="auth-main-content">
            <div class="auth-card-container">
                <div class="auth-card">
                    <h2 class="auth-card-title">ยินดีต้อนรับกลับ</h2>
                    <p class="auth-card-subtitle">โปรดลงชื่อเข้าใช้เพื่อดำเนินการต่อ</p>
                    <form id="login-form">
                        <div class="form-group">
                            <label for="email">อีเมล</label>
                            <input type="email" id="email" name="email" placeholder="กรอกอีเมลของคุณ" required>
                        </div>
                        <div class="form-group">
                            <label for="password">รหัสผ่าน</label>
                            <input type="password" id="password" name="password" placeholder="กรอกรหัสผ่านของคุณ" required>
                        </div>
                        <button type="submit" class="btn btn-gradient">เข้าสู่ระบบ</button>
                    </form>
                    <div class="auth-divider">
                        <hr>
                        <span>หรือ</span>
                        <hr>
                    </div>
                    <p class="auth-switch-link">
                        ยังไม่มีบัญชีใช่ไหม? <a href="/auth/register">ลงทะเบียน</a>
                    </p>
                </div>
            </div>
        </main>
    <!-- Correct public path (Express static usually mounted at /public with / as root for files) -->
    <script src="/js/login.js"></script>
    </body>
</html>