<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style_index.css">
    <link rel="stylesheet" href="/css/user_btn.css">
    <link rel="stylesheet" href="/css/nav.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/card.css">

</head>
    <body>
        <%- include('partials/nav'); %>
        <section class="hero">
            <div class="container">
                <div class="hero-content">
                    <h1>ประตูสู่ความสำเร็จในรั้ว<span>มหาวิทยาลัย</span></h1>
                    <p>ค้นพบกิจกรรมที่น่าตื่นเต้น, เชื่อมต่อกับมหาวิทยาลัยชั้นนำ และก้าวไปสู่ก้าวต่อไปในเส้นทางการศึกษาของคุณ</p>
                    <div class="hero-buttons">
                        <a href="/event" class="hero-btn-primary">สำรวจกิจกรรม</a>
                        <a href="/universities" class="hero-btn-secondary">ดูมหาวิทยาลัย</a>
                    </div>
                </div>
            </div>
        </section>

        <section class="announcements">
            <div class="container">
                <div class="featured-wrapper">
                    <h1>ประกาศล่าสุด</h1>
                    <h3>ข่าวสาร ทุนการศึกษา และการรับสมัครจากมหาวิทยาลัย</h3>
                    <div class="card-container">
                        <% const annSample = (typeof announcements !== 'undefined' && announcements) ? announcements : [
                            {university:'จุฬาลงกรณ์มหาวิทยาลัย', tag:'รับสมัคร', title:'เปิดรับสมัครทุนการศึกษา ประจำปี 2026', deadline:'15 มกราคม 2026',  description:'ทุนสำหรับนักเรียนที่มีผลการเรียนดีและมีความเด่นทางกิจกรรม'} ,
                            {university:'มหาวิทยาลัยมหิดล', tag:'ประกาศ', title:'ประกาศผลการสอบสัมภาษณ์ รอบโควตา', deadline:'28 กุมภาพันธ์ 2026',  description:'ดูรายชื่อและขั้นตอนการยืนยันสิทธิ์ที่เว็บไซต์ของคณะ'},
                            {university:'มหาวิทยาลัยธรรมศาสตร์', tag:'ข่าว', title:'ประชาสัมพันธ์โครงการแลกเปลี่ยนต่างประเทศ', deadline:'10 มีนาคม 2026',  description:'โอกาสไปแลกเปลี่ยน 1 เทอม สำหรับนักศึกษาที่สมัครและผ่านการคัดเลือก'}
                        ]; %>

                        <% annSample.forEach(function(a){ %>
                        <article class="card">
                            <div class="card-header">
                                <div class="tags">
                                    <span class="tag university"><%= a.university %></span>
                                    <span class="tag"><%= a.tag %></span>
                                </div>
                                <h3><%= a.title %></h3>
                            </div>
                            <div class="card-body">
                                <p><i class="fas fa-calendar-alt"></i> วันปิดรับสมัคร / วันที่สำคัญ: <strong><%= a.deadline %></strong></p>
                                <p class="description"><%= a.description %></p>
                            </div>
                            <div class="card-footer">
                                <a href="/announcement" class="btn">ดูเพิ่มเติม</a>
                                <div class="heart-icon">❤</div>
                            </div>
                        </article>
                        <% }) %>

                    </div>
                    <a href="/announcement" class="btn-view-events">ดูประกาศทั้งหมด <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
        </section>

        <section class="featured">
            <div class="container">
                <div class="featured-wrapper">
                    <h1>กิจกรรมเด่น</h1>
                    <h3>อย่าพลาดโอกาสดีๆ จากมหาวิทยาลัยชั้นนำเหล่านี้</h3>
                    <div class="card-container">
                        <% (events || []).forEach(function(ev){ %>
                            <% const tagClass = (ev.tag || '').toLowerCase().includes('openhouse') ? 'openhouse' : (ev.tag || '').toLowerCase().includes('academic') ? 'academic' : (ev.tag || '').toLowerCase().includes('social') ? 'social' : 'university'; %>
                            <div class="card" data-event-id="<%= ev.id %>" data-university="<%= ev.university %>" data-tag="<%= ev.tag %>" data-faculty="<%= ev.faculty || '' %>" data-department="<%= ev.department || '' %>">
                            <div class="card-header">
                                <div class="tags">
                                    <span class="tag <%= tagClass %>"><%= ev.tag %></span>
                                    <span class="tag university"><%= ev.university %></span>
                                </div>
                                <h3><%= ev.title %></h3>
                            </div>
                            <div class="card-body">
                                <p><i class="fas fa-calendar-alt"></i> <%= (new Date(ev.date)).toLocaleDateString('th-TH', { year: 'numeric', month: 'long', day: 'numeric' }) %></p>
                                <% if (ev.time) { %>
                                <p><i class="fas fa-clock"></i> <%= ev.time %></p>
                                <% } %>
                                <% if (ev.location) { %>
                                <p><i class="fas fa-map-marker-alt"></i> <%= ev.location %></p>
                                <% } %>
                                <% if (typeof ev.interested !== 'undefined') { %>
                                <p class="interested"><i class="fas fa-users"></i> <%= ev.interested %> คนสนใจ</p>
                                <% } %>
                                <p class="description"><%= ev.description %></p>
                                </div>
                            <div class="card-footer">
                                <button class="btn join-btn">เข้าร่วมกิจกรรม</button>
                                <i class="far fa-heart heart-icon"></i>
                            </div>
                            </div>
                            <% }) %>
                    </div>
                    <a href="/event" class="btn-view-events">ดูอีเวนต์ทั้งหมด <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
        </section>

        <section class="cta">
            <div class="container">
                <div class="cta-container-wrapper">
                    <h2>พร้อมที่จะเริ่มต้นเส้นทางของคุณหรือยัง?</h2>
                    <p>เข้าร่วมกับนักเรียนมัธยมปลายหลายพันคนที่กำลังสำรวจอนาคตของพวกเขาด้วยแพลตฟอร์มของเรา</p>
                    <a href="/register" class="start-btn">เริ่มเลยวันนี้</a>
                </div>
            </div>
        </section>

        <!-- <%- include('partials/footer') %> -->
    </body>
</html>
