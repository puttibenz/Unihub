<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>มหาวิทยาลัยคู่ค้า</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/css/style_universities.css">
    <link rel="stylesheet" href="/css/user_btn.css">
    <link rel="stylesheet" href="/css/nav.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/card.css">
    
</head>
    <body>
        <%- include('partials/nav'); %>
        
        <section class="page-header">
            <div class="container">
                <div class="header-text">
                    <h1>มหาวิทยาลัยคู่ค้า</h1>
                    <p>สำรวจมหาวิทยาลัยชั้นนำและค้นพบกิจกรรมและโอกาสที่พวกเขามีให้กับนักเรียนมัธยมปลาย</p>
                </div>
            </div>
        </section>

        <section class="search-filter-section">
            <div class="container">
                <div class="search-filter-wrapper">
                    <div class="search-bar">
                        <i class="fas fa-search"></i>
                        <input type="text" placeholder="ค้นหามหาวิทยาลัย, สาขาวิชา, หรือโปรแกรม...">
                    </div>
                    <div class="results-info">
                        <p>พบ 6 มหาวิทยาลัย</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="universities-grid-section">
            <div class="container">
                <div class="card-container">
                    <% if (typeof universities !== 'undefined' && universities && universities.length) { %>
                        <% universities.forEach(function(u){ %>
                            <div class="card">
                                <div class="card-header">
                                    <h3><%= u.name %></h3>
                                </div>
                                <div class="card-body">
                                    <p class="location"><i class="fas fa-map-marker-alt"></i> <%= u.location || '-' %></p>
                                </div>
                                <div class="card-footer">
                                    <a href="/events?universityId=<%= u.id %>" class="btn-primary">ดูกิจกรรม</a>
                                    <% if (u.website) { %>
                                        <a href="<%= u.website %>" target="_blank" rel="noopener" class="btn-secondary">เว็บไซต์</a>
                                    <% } else { %>
                                        <a href="#" class="btn-secondary disabled">เว็บไซต์</a>
                                    <% } %>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="empty-state">
                            <p>ไม่พบมหาวิทยาลัย</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </section>

        <%- include('partials/footer') %>

    </body>
</html>